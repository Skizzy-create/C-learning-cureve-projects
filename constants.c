#include <stdio.h>

int main()
{
    const int num = 69;
    printf("%d",num);

// The **const** can be put before or after the data type

    int const num1 = 420;
    printf("\nthis is the new number:%d", num1);
}